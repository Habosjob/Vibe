# IMPROVEMENTS

## Идея 1
- **Задача:** Добавить расчет YTM и дюрации по каждой облигации.
- **Зачем:** Чтобы ранжировать не только по цене, но и по ожидаемой доходности.
- **Профит простыми словами:** Пользователь сразу видит, какие бумаги потенциально выгоднее.
- **Полезность 1..5:** 5

## Идея 9
- **Задача:** Добавить опцию в `config.yml` для ограничения колонок только на этапе экспорта (без ограничения парсинга).
- **Зачем:** Чтобы хранить весь набор данных, но уметь делать компактный файл для повседневной работы.
- **Профит простыми словами:** Данные не теряются, а в Excel можно оставить только нужное под текущую задачу.
- **Полезность 1..5:** 5

## Идея 11
- **Задача:** Добавить отдельный конфиг для ручного управления группами колонок в Excel (порядок, скрытие, названия групп).
- **Зачем:** Чтобы пользователь мог подстроить макет под свой сценарий без изменения кода.
- **Профит простыми словами:** Можно «собрать» удобную таблицу под себя в пару правок конфига.
- **Полезность 1..5:** 5

## Идея 15
- **Задача:** Вынести правила группировки полей Excel в отдельный YAML-конфиг.
- **Зачем:** Сейчас изменения групп требуют правки кода и нового релиза.
- **Профит простыми словами:** Можно менять расположение колонок «под себя» без программиста.
- **Полезность 1..5:** 5

## Идея 3
- **Задача:** Добавить в итоговый файл отдельный столбец `DATA_STATUS` (ok/warning/error).
- **Зачем:** Чтобы быстро отфильтровывать бумаги с неполными или подозрительными данными.
- **Профит простыми словами:** Пользователь сразу видит, каким строкам можно доверять без ручной проверки.
- **Полезность 1..5:** 4

## Идея 7
- **Задача:** Добавить условное форматирование по `PREVLEGALCLOSEPRICE` (градиент/пороговые цвета).
- **Зачем:** Быстрее визуально находить экстремальные значения цен.
- **Профит простыми словами:** Глаза сразу цепляются за самые дорогие и самые дешевые бумаги.
- **Полезность 1..5:** 4

## Идея 8
- **Задача:** Кешировать ответ первой страницы MOEX на короткий TTL (например 5-10 минут).
- **Зачем:** Сократить число одинаковых запросов при частых ручных перезапусках.
- **Профит простыми словами:** Повторный запуск работает быстрее и стабильнее.
- **Полезность 1..5:** 4

## Идея 10
- **Задача:** Сохранять рядом с Excel JSON-схему колонок текущей выгрузки (`output/moex_bonds.columns.json`).
- **Зачем:** Упростить отслеживание, когда MOEX добавляет или убирает поля.
- **Профит простыми словами:** Пользователь сразу видит, какие колонки появились новыми и что изменилось.
- **Полезность 1..5:** 4

## Идея 13
- **Задача:** Добавить настройку символа для пустых/некорректных дат в Excel (`""` или `"✖"`) через `config.yml`.
- **Зачем:** Разные пользователи по-разному воспринимают пустое значение и явный маркер ошибки.
- **Профит простыми словами:** Можно выбрать, как удобнее видеть «проблемные» даты — пусто или с заметным крестиком.
- **Полезность 1..5:** 4

## Идея 16
- **Задача:** Добавить опцию в конфиг для включения/выключения объединения ячеек заголовков групп в Excel.
- **Зачем:** Разные пользователи по-разному работают с фильтрами/группами и могут предпочитать объединённые или повторяющиеся заголовки.
- **Профит простыми словами:** Можно выбрать более привычный вид таблицы без правки кода.
- **Полезность 1..5:** 4

## Идея 17
- **Задача:** Добавить отдельный параметр `excel.separator_column_width` в `config.yml` для управления шириной столбца-разделителя групп.
- **Зачем:** Сейчас ширина фиксированная, а на разных мониторах и шрифтах пользователю может требоваться больше/меньше места.
- **Профит простыми словами:** Подгоняем вид групп под себя без правки Python-кода.
- **Полезность 1..5:** 4

## Идея 18
- **Задача:** Добавить конфиг `excel.date_fields` для явного списка полей-дата при экспорте.
- **Зачем:** Сейчас определение дат строится по названию поля, а у редких полей это может быть неочевидно.
- **Профит простыми словами:** Пользователь сам задает, какие колонки должны вести себя как даты и группироваться по годам.
- **Полезность 1..5:** 4

## Идея 20
- **Задача:** Добавить отдельный диагностический флаг `excel.debug_cell_types`, который сохраняет рядом CSV с типами ячеек (`number/text/date`) по ключевым колонкам.
- **Зачем:** Быстрее понимать, почему в конкретной выгрузке Excel сортирует поле как текст.
- **Профит простыми словами:** Можно сразу увидеть, где число «сломалось» в текст, и не гадать по виду таблицы.
- **Полезность 1..5:** 4

## Идея 2
- **Задача:** Сделать простое GUI (Tkinter) с кнопкой "Обновить данные".
- **Зачем:** Упростить запуск для пользователя далекого от IT.
- **Профит простыми словами:** Не нужен терминал — нажал кнопку и получил таблицу.
- **Полезность 1..5:** 4

## Идея 5
- **Задача:** Сохранять версионные копии результатов в `output/archive/` с датой и временем.
- **Зачем:** Чтобы можно было сравнивать текущий скрин с прошлым без ручного копирования.
- **Профит простыми словами:** Легко отследить, как менялся рынок по запускам.
- **Полезность 1..5:** 3

## Идея 12
- **Задача:** Добавить в Excel легенду по группам и цветам на отдельный лист `HELP`.
- **Зачем:** Чтобы новым пользователям было проще понять структуру отчета.
- **Профит простыми словами:** Даже без IT-опыта сразу понятно, где цены, где купоны и где даты.
- **Полезность 1..5:** 3

## Идея 14
- **Задача:** Добавить авто-подбор ширины колонок по первым N строкам с ограничением по времени.
- **Зачем:** На больших выгрузках текущий полный проход по всем строкам может замедлять экспорт.
- **Профит простыми словами:** Файл формируется быстрее, а ширина колонок остается читаемой.
- **Полезность 1..5:** 3

## Идея 21
- **Задача:** Добавить ручной `manual_exclusions.yml` для принудительного исключения конкретных `SECID` по пользовательским причинам.
- **Зачем:** Иногда бумагу нужно убрать не по датам, а по внутренним правилам портфеля.
- **Профит простыми словами:** Можно «забанить» бумагу без правок кода и без ожидания нового релиза.
- **Полезность 1..5:** 5

## Идея 22
- **Задача:** Вынести статистику фильтров в отдельный лист `FILTERS` с историей по запускам.
- **Зачем:** Сейчас видно только один снимок, сложно отследить динамику исключений.
- **Профит простыми словами:** Пользователь видит, какие причины фильтрации становятся частыми со временем.
- **Полезность 1..5:** 4
