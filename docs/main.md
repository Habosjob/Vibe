# Скрипт `main.py` — загрузка MOEX rates + витрина эмитентов

## Что делает скрипт
1. Загружает CSV с MOEX по ссылке из `config.py`.
2. Сохраняет файл в папки:
   - `raw/moex_rates.csv` (основной «сырой» файл),
   - `cache/moex_rates_cache.csv` (кэш-копия).
3. Обновляет SQLite-базу `DB/moex_rates.sqlite3` (таблица `rates`) полной перезаливкой при истекшем TTL.
4. Ведет таблицу `emitents` с уникальными эмитентами по `INN`:
   - берет поля `EMITENTNAME` и `INN` из `rates`,
   - инкрементально делает upsert (новые INN добавляются, у существующих обновляется имя эмитента),
   - хранит пользовательские поля `Scoring` и `DateScoring`.
5. Синхронизирует ручные оценки из Excel-витрины `Emitents.xlsx` обратно в SQL:
   - пользователь заполняет `Scoring` вручную в Excel,
   - скрипт переносит эти значения в таблицу `emitents`,
   - значения не затираются при обычном обновлении данных из MOEX.
6. Автоматически заполняет `DateScoring`, когда есть `Scoring`, а дата еще пустая.
7. Формирует Excel-витрину `Emitents.xlsx` в корне проекта с оформлением:
   - жирные заголовки,
   - цветные заголовки,
   - автофильтр,
   - закрепление первой строки,
   - автоширина колонок,
   - жесткая проверка `Scoring` через выпадающий список (`Red`, `Yellow`, `Green`).
8. Создает снапшоты в `BaseSnapshots`:
   - `rates_snapshot.xlsx` (5 случайных строк с уникальными `SECID`),
   - `emitents_snapshot.xlsx` (5 случайных строк из `emitents`).
9. Пишет технический лог в `logs/main.log` (перезаписывается каждый запуск).

## Запуск
```bash
python main.py
```

> Без CLI/argparse: просто запуск `main.py`.

## Что выводится в консоль
В консоль выводятся только этапы, прогресс-бары и итоговый summary по времени.
Техническая детализация уходит в `logs/main.log`.

## Логика инкремента по таблице `emitents`
- Ключ уникальности: `INN`.
- Источник: таблица `rates`.
- Инкрементальное обновление:
  - `INSERT ... SELECT DISTINCT ... ON CONFLICT(INN) DO UPDATE`.
- Поля, которые не теряются между запусками:
  - `Scoring`,
  - `DateScoring`.

## Логика `Scoring` и `DateScoring`
- `Scoring`:
  - заполняется вручную пользователем в `Emitents.xlsx`,
  - доступен только выбор из выпадающего списка: `Red`, `Yellow`, `Green`,
  - ввод других значений блокируется в Excel,
  - если некорректное значение все же попало в файл (например, через внешний импорт), скрипт не переносит его в SQL и пишет предупреждение в лог,
  - переносится в SQL при следующем запуске.
- `DateScoring`:
  - если у строки есть `Scoring`, но нет `DateScoring`, скрипт ставит текущую дату,
  - формат даты задается параметром `DATE_SCORING_FORMAT`.

## Настройка
Все настройки находятся в `config.py`.
Каждый параметр снабжен комментарием: назначение, допустимые значения и дефолт.

Ключевые параметры:
- `CACHE_TTL_HOURS` — срок жизни кэша в часах.
- `REQUEST_TIMEOUT_SECONDS` — таймаут HTTP.
- `SOURCE_CSV_URL` — источник данных.
- `EMITENTS_TABLE_NAME` — таблица витрины эмитентов в SQLite.
- `EMITENTS_XLSX_FILENAME` — имя Excel-витрины в корне проекта.
- `EMITENTS_HEADER_FILL_COLOR` — цвет заголовков Excel.
- `SCORING_ALLOWED_VALUES` — разрешенные значения в выпадающем списке `Scoring`.
- `DATE_SCORING_FORMAT` — формат даты для `DateScoring`.
- `EMITENTS_SNAPSHOT_FILENAME` — имя снапшота эмитентов в `BaseSnapshots`.

## Примечание для Windows
Проект ориентирован на Windows-сценарий:
- пути собираются через `pathlib.Path`,
- лог/файлы пишутся в UTF-8,
- запуск без дополнительных CLI-аргументов.
