## 2026-02-19 (обновление 41)

### Добавлено
- Добавлен расчёт `YIELD` по денежным потокам (без реинвестирования купонов) с использованием цены покупки из `TOTAL_PRICE`.
- При наличии даты оферты (`PUT_CALL_OFFER_DATE`) доходность считается до даты оферты, как до даты погашения.
- Для бумаг с амортизацией в расчёт включаются частичные возвраты номинала из графика `bondization` MOEX.

### Изменено
- Поле `YIELD` теперь приоритетно заполняется расчётным значением по cash-flow модели вместо биржевого значения.

### Добавлено (тесты)
- Добавлены тесты для расчёта доходности по денежным потокам и для сценария с офертой/`TOTAL_PRICE`.

### Документация
- Обновлены `README.md` и описание поля `YIELD` на листе `INFO`: добавлены понятные правила нового расчёта.

## 2026-02-19 (обновление 40)

### Исправлено
- Расчёт `TOTAL_PRICE` теперь выполняется только для бумаг с `FACEUNIT = SUR`.
- Для бумаг с другой валютой номинала поле `TOTAL_PRICE` принудительно очищается, а запись удаляется из кэша `TOTAL_PRICE`.

### Изменено
- Версия схемы кэша `TOTAL_PRICE` повышена до `v3`, чтобы автоматически пересобрать кэш по новым правилам (`FACEUNIT = SUR`).
- Проверка кэша `TOTAL_PRICE` теперь учитывает `FACEUNIT` как часть условий валидности.

### Добавлено (тесты)
- Добавлен тест, что для `FACEUNIT != SUR` значение `TOTAL_PRICE` не рассчитывается и запись удаляется из кэша.
- Обновлены тесты расчёта/кэша `TOTAL_PRICE` с явным указанием `FACEUNIT`.

### Документация
- Обновлён `README.md` и описание поля `TOTAL_PRICE` на листе `INFO` с новым обязательным условием `FACEUNIT = SUR`.

## 2026-02-19 (обновление 39)

### Добавлено
- Добавлен отдельный кэш `cache/total_price_cache.json` для столбца `TOTAL_PRICE`.
- В кэше `TOTAL_PRICE` используется версия схемы (`schema_version`): при изменении формата старый кэш автоматически очищается и пересобирается.

### Изменено
- `TOTAL_PRICE` теперь берётся из кэша, если входные поля не изменились (`FACEVALUE`, `PREVPRICE`, `ACCRUEDINT`, `PREVLEGALCLOSEPRICE`, `VOLTODAY`, `SECID`).
- Если бумага перестала проходить условия расчёта (`VOLTODAY <= 0` или нулевые/пустые `PREVLEGALCLOSEPRICE`/`PREVPRICE`), её запись удаляется из кэша `TOTAL_PRICE`.

### Добавлено (тесты)
- Добавлены тесты на использование кэша `TOTAL_PRICE` при неизменных входных данных.
- Добавлен тест автоочистки `total_price_cache.json` при старой версии схемы.

### Документация
- Обновлён `README.md`: добавлен шаг про кэш `TOTAL_PRICE` и обновлён список кэшей проекта.

## 2026-02-19 (обновление 38)

### Исправлено
- Уточнены правила расчёта `TOTAL_PRICE`: значение считается только если `VOLTODAY > 0`, а `PREVLEGALCLOSEPRICE` и `PREVPRICE` не пустые и не равны нулю.
- Исправлена бизнес-формула в явном виде: рыночная цена (`FACEVALUE * PREVPRICE / 100`) + НКД (`ACCRUEDINT`) + комиссия 0.05% от суммы (рыночная цена + НКД).
- Для строк, где условия расчёта не выполнены, `TOTAL_PRICE` теперь остаётся пустым (без артефактов в отчёте).

### Добавлено (тесты)
- Добавлены тесты на проверку условий расчёта `TOTAL_PRICE` и вспомогательной валидации ненулевых значений.

### Документация
- Обновлён `README.md` и описание поля `TOTAL_PRICE` на листе `INFO`: добавлены понятные условия расчёта и пояснение, когда поле остаётся пустым.

## 2026-02-19 (обновление 37)

### Добавлено
- Добавлен расчёт нового столбца `TOTAL_PRICE` по формуле `((FACEVALUE * PREVPRICE / 100) + ACCRUEDINT) * 1.0005`.
- Новая колонка `TOTAL_PRICE` включена в секцию `Рынок` на листе `MOEX_BONDS` и в лист `INFO` с понятным описанием.

### Добавлено (тесты)
- Добавлены автотесты на формулу `TOTAL_PRICE` и на заполнение нового поля в строках выгрузки.

### Документация
- Обновлён `README.md`: добавлен шаг расчёта `TOTAL_PRICE` и описание поля для пользователя.

## 2026-02-19 (обновление 26)

### Исправлено
- Исправлена причина, почему `ISSUER_RATING` был `Нет данных на MOEX` почти для всех бумаг: данные рейтингов часто отсутствуют в `description`, но доступны в CCI API MOEX.
- Добавлен fallback на CCI API (`/iss/cci/rating/companies/ecbd_<EMITTER_ID>...`): если рейтинг не найден в карточке бумаги, скрипт берёт его по эмитенту и ISIN.
- Для примера `RU000A0JTU85` рейтинг теперь корректно определяется (например, `АКРА: AAA(RU)` и др. значения по доступным агентствам).

### Изменено
- Нормализация рейтингов теперь собирает актуальные значения по агентствам в одну строку (`АГЕНТСТВО: РЕЙТИНГ`).
- Добавлена версия схемы кэша рейтингов эмитентов (`issuer_rating_schema_version`) — старые ошибочные значения автоматически переобогащаются.

### Добавлено (тесты)
- Добавлены тесты на сбор рейтингов из CCI-строк и на fallback-логику получения рейтинга эмитента.

### Документация
- Обновлены `README.md` и лист `INFO` с пояснением двухшагового источника `ISSUER_RATING` (карточка MOEX + CCI fallback).

## 2026-02-19 (обновление 25)

### Исправлено
- Уточнено поведение поля `ISSUER_RATING`: теперь оно не остаётся пустым.
- Если MOEX не передаёт рейтинг эмитента в карточке бумаги, в отчёте и кэше ставится понятное значение `Нет данных на MOEX`.

### Документация
- Обновлены `README.md` и лист `INFO` с пояснением нового значения по умолчанию для `ISSUER_RATING`.

## 2026-02-19 (обновление 24)

### Добавлено
- Добавлено поле `ISSUER_RATING`: скрипт пытается взять рейтинг эмитента из карточки бумаги MOEX (`description`), если биржа его публикует.
- Рейтинг сохраняется в кэш эмитентов и в checkpoint этапа обогащения, чтобы не перезапрашивать одно и то же при следующих запусках.

### Изменено
- Блок `Эмитент` на листе `MOEX_BONDS` расширен колонкой `ISSUER_RATING`.
- Лист `INFO` и README обновлены: добавлено понятное описание нового поля и поведение при пустом значении.

## 2026-02-19 (обновление 36)

### Добавлено
- Новый этап расчёта `COUPONVALUE`: если MOEX вернул `COUPONVALUE = 0`, но есть `COUPONPERCENT`, `FACEVALUE` и `COUPONPERIOD`, значение считается по формуле `FACEVALUE * COUPONPERCENT / 100 / 365 * COUPONPERIOD`.
- Добавлен отдельный кэш `cache/coupon_value_cache.json` для расчётного `COUPONVALUE`.

### Изменено
- Расчётный `COUPONVALUE` теперь подсвечивается жёлтым в Excel (по аналогии с расчётным `COUPONPERCENT`).
- Логика кэширования `COUPONVALUE` учитывает изменение входных данных: при изменении `COUPONPERCENT`, `FACEVALUE`, `COUPONPERIOD` или `SECID` значение пересчитывается.

### Добавлено (тесты)
- Тесты на формулу и кэширование `COUPONVALUE`: проверка прямого расчёта, использования кэша при неизменных входных данных и пересчёта при изменении `COUPONPERCENT`.

### Документация
- Обновлён `README.md`: добавлено описание нового этапа и нового кэша расчётного `COUPONVALUE`.

## 2026-02-19 (обновление 35)

### Изменено
- Обновлён внешний вид групповых колонок в итоговом Excel: убрано объединение ячеек в строках 1-2, чтобы шапка была «плоской» и удобной для разворачивания групп.
- Заголовки групповых колонок переведены в горизонтальный режим с переносом текста (`wrap_text=True`) вместо вертикального поворота.
- Увеличена ширина групповых колонок-разделителей (с `7` до `15`) и повышена высота верхней строки (до `78`), чтобы длинные подписи отображались читаемо.

## 2026-02-19 (обновление 34)

### Исправлено
- По задаче пользователя из проверки оферт полностью исключён ДОХОД: внешняя валидация оферт теперь выполняется только через Corpbonds (MOEX остаётся первичным источником).
- Версия схемы кэша оферт повышена до `v8`, чтобы автоматически сбросить устаревшие записи после изменения источников и логики.
- Усилен парсинг даты оферты Corpbonds: после метки «Дата ближайшей оферты» скрипт просматривает несколько соседних строк, поэтому дата корректно находится даже при нестандартной вёрстке страницы.
- Исправлен кейс с пустыми офертами для `RU000A0JT403`: дата оферты корректно определяется как `2027-09-28` по MOEX и Corpbonds.

### Добавлено
- Добавлен тест `test_parse_corpbonds_offer_metrics_finds_date_in_nearby_lines` на поиск даты оферты в соседних строках HTML.

### Документация
- Обновлён `README.md`: шаги проверки оферт приведены к новой схеме `MOEX -> Corpbonds`.

## 2026-02-19 (обновление 33)

### Исправлено
- Исправлен сценарий `include_external_offers=False`: добавлена безопасная инициализация переменных оферты внутри `enrich_with_daily_metrics`, чтобы исключить `UnboundLocalError` при обработке строк.
- Для гарантированной очистки старых кэшей после исправления версия схемы кэша оферт повышена до `v7`.

### Добавлено
- Добавлен регрессионный тест `test_enrich_with_daily_metrics_no_external_offer_does_not_fail`, который проверяет, что этап обогащения не падает без внешних оферт и корректно проставляет `✔/✖`.

### Документация
- Обновлён changelog по фактическому исправлению падения.

## 2026-02-19 (обновление 32)

### Изменено
- Логика оферт возвращена к формату "есть/нет": в `HAS_PUT_CALL_OFFER` теперь сохраняется `✔` или `✖` (без деления на `PUT`/`Call`).
- Внешняя проверка через ДОХОД/Corpbonds теперь выполняется только для бумаг, где MOEX не нашёл оферту после первичного этапа.
- Объединение внешних источников ужесточено: оферта учитывается только при наличии валидной даты, иначе считается отсутствующей.

### Исправлено
- Устранён кейс "тип оферты без даты": при пустой дате флаг оферты автоматически ставится в `✖`.
- Для сброса старых значений изменена версия схемы кэша оферт на `v6`.

### Добавлено
- Тест `test_select_offer_jobs_for_refresh_checks_only_moex_without_offer` (внешняя проверка только для бумаг без оферты по MOEX).

### Документация
- Обновлён `README.md` с новым правилом по полю `HAS_PUT_CALL_OFFER` и последовательности этапов проверки оферт.

## 2026-02-19 (обновление 31)

### Исправлено
- Добавлено строгое правило целостности оферт: если дата оферты отсутствует, значение `HAS_PUT_CALL_OFFER` теперь принудительно сбрасывается в `✖` (тип без даты больше не остаётся в выгрузке).
- В объединении внешних источников (`merge_offer_metrics`) оферта теперь принимается только если есть и подтверждённый тип (`PUT/Call`), и валидная дата; иначе запись считается отсутствием оферты.
- Для очистки старых некорректных значений версия схемы кэша оферт повышена до `v5`, старый кэш автоматически удаляется при чтении.

### Добавлено
- Добавлен тест `test_merge_offer_metrics_requires_date_for_offer_type` (тип без даты не проходит).
- Добавлен тест `test_parse_corpbonds_offer_metrics_detects_call` для явной проверки распознавания `Call` в парсере Corpbonds.

### Документация
- Обновлён `README.md` с пояснением правила «нет даты — нет оферты».

## 2026-02-19 (обновление 30)

### Исправлено
- Убрана ложная генерация `PUT` по одной только дате: если внешний источник не подтвердил тип оферты (`PUT`/`Call`), оферта теперь не создаётся.
- Для ДОХОД отключено использование поля «Дата, к которой рассчитана YTM» как даты оферты: теперь берётся только «Дата ближайшей оферты».
- Добавлена защитная нормализация итоговых данных: если дата оферты совпадает с `MATDATE`, запись считается погашением, а не офертой (`HAS_PUT_CALL_OFFER=✖`, дата очищается).
- Для очистки ранее накопленных ложных значений версия схемы кэша оферт повышена до `v4`, старый кэш автоматически удаляется и пересобирается.

### Добавлено
- Новые тесты: проверка, что дата без типа не превращается в `PUT`, и что YTM-дата не трактуется как оферта.

### Документация
- Обновлено описание логики кэша/оферт в `README.md`.

## 2026-02-19 (обновление 29)

### Исправлено
- По запросу пользователя принудительно сброшен старый кэш оферт: версия схемы `offer_verification_cache` увеличена до `v3`, поэтому кэш предыдущего формата автоматически удаляется и пересобирается заново при первом запуске.

### Добавлено
- Добавлен тест `test_load_offer_verification_cache_clears_old_schema`, который проверяет автоочистку старого кэша оферт при несовпадении версии схемы.

### Документация
- Обновлён changelog с явным описанием механизма сброса старого кэша оферт.

## 2026-02-19 (обновление 28)

### Исправлено
- Добавлен расширенный контроль качества оферт перед выгрузкой: в лог теперь выводятся три счётчика — количество бумаг с офертами, количество бумаг с офертами без даты и количество бумаг, где дата оферты совпадает с датой погашения.

### Добавлено
- Добавлен автотест `test_validate_rows_logs_offer_quality_counters`, который проверяет корректность новых счётчиков качества оферт в логе.

### Документация
- Обновлён `README.md` с пояснением нового контроля оферт на этапе проверки качества.
- Реализованный пункт `IMPROVEMENTS.md` №4 перенесён в changelog и удалён из backlog.

## 2026-02-19 (обновление 27)

### Изменено
- Полностью отключён парсинг `ISSUEDATE`: поле больше не запрашивается, не валидируется и не выводится в итоговый Excel.
- Реализована идея из `IMPROVEMENTS.md` №0: добавлено поле `COUPON_FORMULA_SOURCE` с источником формулы расчётного купона (`DOHOD` или `CORPBONDS`).

### Документация
- Обновлены `README.md` и `IMPROVEMENTS.md`: реализованные пункты перенесены в changelog, в backlog оставлены только актуальные идеи.

## 2026-02-19 (обновление 26)

### Исправлено
- Приведена в порядок нумерация этапов в логах: убраны конфликтующие значения (`/9`, `5.05`, `5.15`), теперь прогресс читается последовательно по шкале `1/8`.
- Добавлен автоматический сброс старого кэша оферт: при обнаружении старого формата `cache/offer_verification_cache.json` файл стирается и собирается заново.

### Изменено
- В кэш оферт добавлено поле версии схемы (`schema_version`), чтобы безопасно обновлять формат без ручной очистки файлов.

### Документация
- Обновлён `README.md`: исправлена нумерация шагов и добавлено пояснение про автоочистку старого кэша оферт.

### Улучшено
- Реализована идея из `IMPROVEMENTS.md` №11 (управляемый сброс устаревших кэшей), теперь для оферт это происходит автоматически.

## 2026-02-18 (обновление 25)

### Исправлено
- Исправлен парсинг оферт MOEX: теперь корректно определяется тип `PUT`/`Call`, а fallback больше не превращает все оферты в `PUT`.
- Убраны ложные оферты с датой погашения: события `Погашение`/`Redemption`/`Maturity` больше не считаются офертой.

### Добавлено
- Добавлены локальные тесты `test_offer_parsing.py` для проверки нормализации типа оферты и фильтрации ложных записей.

## 2026-02-18 (обновление 24)

### Исправлено
- Этап обогащения `ISSUEDATE` перенесён на правильное место: теперь дата начала торгов запрашивается **после фильтра по `COUPONPERIOD`**, чтобы не делать лишние запросы по бумагам, которые всё равно исключаются.

### Изменено
- Кэш `cache/trade_start_date_cache.json` закреплён как пожизненный для `ISSUEDATE` (без TTL): ранее сохранённые даты используются между запусками и не перезапрашиваются без необходимости.

### Документация
- Обновлён `README.md` с новым порядком этапов и пояснением по пожизненному кэшу `ISSUEDATE`.

## 2026-02-18 (обновление 22)

### Исправлено
- Этап обогащения `ISSUEDATE` перенесён перед внешними источниками (DOHOD/Corpbonds), чтобы поле получалось раньше и не тормозило поздние этапы.
- Добавлен checkpoint `cache/trade_start_date_checkpoint.json` для `ISSUEDATE`: прогресс сохраняется каждые 25 бумаг, поэтому при обрыве связи этап корректно продолжается с сохранённых данных.

### Изменено
- Увеличен параллелизм этапа `ISSUEDATE` (`TRADE_START_MAX_WORKERS=20`) для ускорения массового дозапроса карточек бумаг по `SECID`.

### Документация
- Обновлён `README.md`: добавлено описание нового порядка этапов и checkpoint-механизма для `ISSUEDATE`.

## 2026-02-18 (обновление 23)

### Исправлено
- Исправлена причина отсутствия столбца `ISSUEDATE` в части запусков: поле не приходит в общем списке `.../markets/bonds/securities.json`, поэтому добавлено отдельное обогащение через карточку `.../iss/securities/<SECID>.json`.

### Добавлено
- Добавлен отдельный кэш `cache/trade_start_date_cache.json` для дат начала торгов, чтобы не запрашивать одну и ту же дату при каждом запуске.
- Добавлен прогресс-лог по этапу загрузки `ISSUEDATE` и контроль качества (количество пустых `ISSUEDATE`).

### Документация
- Обновлён `README.md` с пояснением, откуда берётся `ISSUEDATE` и почему это отдельный этап.

## 2026-02-18 (обновление 22)

### Добавлено
- В основной запрос MOEX ISS добавлено поле `ISSUEDATE` (дата начала торгов выпуска), поле сохраняется в итоговый Excel.

### Изменено
- Блок колонок в Excel расширен и переименован в `Торги, погашение и амортизация`: теперь в нём есть `ISSUEDATE`, `HAS_AMORTIZATION`, `AMORTIZATION_START_DATE`, `MATDATE`.
- Форматирование дат расширено: `ISSUEDATE` приводится к формату `ДД.ММ.ГГГГ`, как и остальные ключевые даты отчёта.

### Документация
- Обновлены `README.md` и лист `INFO` с понятным описанием нового поля `ISSUEDATE`.

## 2026-02-18 (обновление 21)

### Исправлено
- Убрана проблема «раздувания» потоков на этапе внешней проверки оферт: удалены вложенные пуллы потоков, введён фиксированный лимит `OFFER_SOURCE_MAX_WORKERS`.
- Перестроен порядок этапов: сначала сбор данных MOEX и первичная фильтрация (погашение/амортизация/оферты), затем обогащение внешними источниками, после чего повторная фильтрация по обновлённым данным.

### Изменено
- Параллельная проверка ДОХОД и Corpbonds сохранена, но теперь выполняется в одном ограниченном пуле задач (стабильнее под большой объём ISIN).
- Лимит потоков внешней проверки снижен (`OFFER_CHECK_MAX_WORKERS=12`, `OFFER_SOURCE_MAX_WORKERS=6`) для предотвращения зависаний.

### Документация
- Обновлён `README.md` с новым порядком этапов и объяснением ограниченного параллелизма.

## 2026-02-18 (обновление 20)

### Изменено
- Для каждого ISIN проверка оферт через `analytics.dohod.ru` и `corpbonds.ru` теперь запускается параллельно (оба источника опрашиваются одновременно).
- Правило конфликта по офертам зафиксировано в коде: если хотя бы один источник показывает оферту, итоговый статус — «оферта есть».
- Для проверки оферт убрано лишнее переиспользование общей сессии между потоками: каждый источник получает отдельную HTTP-сессию внутри своей задачи.

### Документация
- Обновлён `README.md`: добавлено понятное описание новой параллельной схемы опроса оферт и правила обработки конфликтов источников.
- Уточнено правило расчёта `COUPONPERCENT`: если формула найдена в одном источнике, второй источник для этого выпуска не опрашивается.

## 2026-02-18 (обновление 19)

### Исправлено
- Устранено повторное замедление: внешний этап оферт переведён на «скользящее обновление» (частичная пере-проверка за запуск), чтобы не перепроверять весь пул ISIN разом.
- Добавлен дневной лимит пере-проверки оферт с гарантией полного обновления списка в течение 7 дней.

### Изменено
- Снижена нагрузка на сеть и логи: предупреждения по внешним источникам теперь агрегируются на уровне результата, без лишнего потока сообщений по каждой попытке.
- Обновлён `README.md` с описанием новой быстрой стратегии обновления оферт.

## 2026-02-18 (обновление 18)

### Исправлено
- Ускорен этап проверки оферт: добавлены отдельные «быстрые» таймауты/ретраи и увеличен параллелизм для внешней проверки (`DOHOD`/`Corpbonds`).
- Добавлен fallback для Corpbonds: если карточка не найдена по ISIN (`404`), автоматически выполняется повторная попытка по `SECID`.
- Улучшен парсинг оферт Corpbonds, чтобы корректно различать `Call` и `PUT` без ложных срабатываний.

### Изменено
- Обновлён `README.md` с описанием ускоренной проверки оферт и fallback-логики `ISIN -> SECID`.

## 2026-02-18 (обновление 17)

### Добавлено
- Добавлена внешняя проверка оферт для отфильтрованных облигаций через `analytics.dohod.ru` и `corpbonds.ru`.
- Добавлен отдельный кэш проверки оферт `cache/offer_verification_cache.json` с TTL **7 дней**.
- Поле `HAS_PUT_CALL_OFFER` переведено в новый формат значений: `PUT`, `Call` или `✖`.

### Изменено
- После проверки оферт теперь повторно применяется фильтр по правилу: до оферты должно оставаться более 1 года.
- В `README.md` и листе `INFO` обновлены описания логики оферт и нового формата значений.

## 2026-02-18 (обновление 16)

### Добавлено
- Добавлен fallback-источник формулы купона: если `analytics.dohod.ru` не вернул формулу, скрипт автоматически пробует `corpbonds.ru/bond/<ISIN>`.
- Для corpbonds.ru добавлен парсинг формул вида `∑KC + X%` и `∑RUONIA + X%` с автоматическим преобразованием `KC/КС` в `CBR_RATE`.

### Изменено
- Многопоточный этап расчёта `COUPONPERCENT` теперь работает в связке `ДОХОД → CORPBONDS` и пишет прогресс в лог как единый этап.
- Лист `INFO` и `README.md` обновлены: жёлтые ячейки `COUPONPERCENT` теперь описаны как расчёт из двух источников (основной + fallback).

## 2026-02-18

### Добавлено
- Добавлен расчёт `COUPONPERCENT` для выпусков с `COUPONVALUE = 0` через формулу с analytics.dohod.ru (`CBR_RATE`, `RUONIA`, `G_CURVE_RUS` + спред).
- Добавлен кэш расчётных купонов `cache/coupon_formula_cache.json` с TTL 7 дней и авто-очисткой устаревших записей.
- Добавлена визуальная подсветка жёлтым цветом ячеек `COUPONPERCENT`, которые рассчитаны, а не получены напрямую с MOEX.

### Изменено
- Добавлен автоматический сбор базовых индексов для расчётов: `CBR_RATE` и `RUONIA` из ЦБ РФ, `G_CURVE_RUS` из MOEX ZCYC.
- Добавлена ранняя перезапись кэша: если MOEX начал отдавать ненулевой `COUPONPERCENT`, расчётный кэш по выпуску сбрасывается.
- Обновлены `README.md` и лист `INFO` (описание жёлтых ячеек как приблизительного расчёта).

### Улучшено
- Реализована новая логика для задач пользователя по плавающему купону и сроку хранения расчётных значений.

# Changelog

## 2026-02-18 (обновление 15)

### Исправлено
- Исправлена проблема пропуска части флоатеров при расчёте `COUPONPERCENT`: усилен парсинг блока «Привязка к индексу» и добавлен fallback-поиск формулы по HTML.
- Добавлены дополнительные повторы запроса страницы ДОХОД для временных сетевых сбоев (в том числе для кейсов вроде `RU000A10B2X0`).

### Изменено
- Этап получения формул с analytics.dohod.ru переведён на многопоточную обработку (`ThreadPoolExecutor`) с прогресс-логами.
- Сохранение кэша формул сделано более устойчивым: папка `cache` гарантированно создаётся перед записью файла.

### Проверка
- Контрольный кейс: `RU000A10B2X0` присутствует в результате, `COUPONPERCENT` рассчитан как `20.5` и подсвечен жёлтым в Excel.

## 2026-02-18 (обновление 14)

### Добавлено
- Добавлен парсинг биржевой доходности `YIELD` из блока `marketdata` MOEX ISS.
- Поле `YIELD` добавлено в итоговый Excel (группа `Рынок`) и на лист `INFO` с понятным описанием.

### Проверка
- Контрольное значение: для бумаги `RU000A106RR9` поле `YIELD` возвращает `14.94`.

## 2026-02-18 (обновление 13)

### Исправлено
- Исправлена проблема «призрачной» колонки `ISSUER_BOND_CLASS` в Excel после отката: теперь перед сохранением данные очищаются от устаревших полей из старого кэша.

### Изменено
- Добавлена служебная очистка удалённых колонок (`ISSUER_BOND_CLASS`, `ISSUER_SECTOR`, `SECTORID`, `INSTRID`) перед сохранением кэша, raw и Excel.
- Обновлён `README.md` с пояснением автоматической очистки старых колонок.

## 2026-02-18 (обновление 12)

### Изменено
- По запросу пользователя полностью удалены поля классификации эмитента: `ISSUER_BOND_CLASS` (и ранее удалённый `ISSUER_SECTOR`) больше не формируются и не попадают в итоговый Excel.
- В группе `Эмитент` оставлены только `ISSUER_NAME`, `ISSUER_INN`, `SHORTNAME`.
- Упрощён запрос к MOEX: убраны неиспользуемые поля `SECTORID` и `INSTRID`.

### Улучшено
- Отчёт возвращён к более стабильному и понятному составу колонок без спорной классификации.

## 2026-02-18 (обновление 11)

### Добавлено
- Добавлено поле `ISSUER_BOND_CLASS` для типа бумаги (государственный, корпоративный, муниципальный/субфедеральный, иностранный).

### Изменено
- Поле `ISSUER_SECTOR` удалено из итоговой выгрузки по запросу пользователя.
- В группе `Эмитент` и на листе `INFO` оставлено только поле `ISSUER_BOND_CLASS` как единый источник классификации типа бумаги.
- Обновлены `README.md` и описание колонок в Excel.

### Улучшено
- Упрощена структура отчёта: исключено дублирование и спорная эвристика отраслей по названию эмитента.

## 2026-02-17 (обновление 10)

### Добавлено
- Добавлен парсинг объёма торгов из блока `marketdata`: теперь в итоговую таблицу попадают поля `VOLTODAY` (объём), `VALTODAY` (оборот) и `NUMTRADES` (количество сделок).
- Лист `INFO` дополнен понятными описаниями новых рыночных полей.

### Изменено
- Группа `Рынок` на листе `MOEX_BONDS` расширена новыми колонками ликвидности.
- Обновлён `README.md`: добавлено описание этапа парсинга объёма торгов и новых колонок в отчёте.

### Улучшено
- Реализована часть идеи по анализу ликвидности: собраны базовые метрики торгов, на основе которых можно добавить фильтр минимального объёма в следующих обновлениях.

## 2026-02-17 (обновление 9)

### Изменено
- Подписи групп перенесены в столбцы-разделители с вертикальным текстом и объединением ячеек 1-2 строки: это улучшает читаемость в сложенном виде.
- Столбцы-разделители расширены, чтобы названия групп не выглядели сжатыми.
- Для разделителей добавлена палитра из разных оттенков, чтобы соседние блоки визуально не сливались.

## 2026-02-17 (обновление 8)

### Исправлено
- Исправлена проблема, из-за которой кнопки сворачивания были не у всех групп колонок: теперь группы формируются как непрерывные блоки.

### Добавлено
- Добавлены отдельные столбцы-разделители между группами колонок на листе `MOEX_BONDS`.
- Названия групп перенесены в столбцы-разделители (по макету), чтобы блоки легче читались визуально.

### Изменено
- Настройка outline для Excel пересобрана: каждая группа получает собственную кнопку складывания/раскладывания.

## 2026-02-17 (обновление 7)

### Исправлено
- Исправлена ошибка повреждения файла при открытии `output/moex_bonds.xlsx` в Microsoft Excel после добавления групп колонок.
- Убрано объединение ячеек в строке подписей групп (именно оно приводило к конфликтам структуры листа в части случаев).

### Изменено
- Подписи групп теперь проставляются в каждой колонке внутри группы, что сохраняет удобство навигации и совместимость с Excel.

## 2026-02-17 (обновление 6)

### Добавлено
- Добавлены новые группы колонок для сворачивания/разворачивания на листе `MOEX_BONDS`: `Квалификация и тип`, `Оферты`, `Погашение и амортизация`, `Купоны`, `Рынок` (к ранее существующей группе `Эмитент`).
- Добавлена отдельная верхняя строка с подписями групп, чтобы в Excel было проще ориентироваться в блоках данных.

### Изменено
- Заголовок таблицы перенесён на вторую строку листа, а закрепление/фильтрация адаптированы под новую структуру с группами.
- Обновлён `README.md` с понятным описанием всех групп колонок и их назначения.

## 2026-02-17 (обновление 5)

### Добавлено
- Добавлен отдельный фильтр по дате оферты: бумаги исключаются, если `PUT_CALL_OFFER_DATE` наступает раньше, чем через 1 год от текущей даты.
- В служебные поля (и суточный кэш) добавлены признаки `EXCLUDE_BY_OFFER_DATE` и `OFFER_DATE_EXCLUDE_REASON`.

### Изменено
- Логика этапов обновлена: фильтр по офертам выполняется сразу после фильтра амортизации и до обогащения эмитентами.
- Обновлён `README.md` с описанием нового правила исключения облигаций по дате оферты.

## 2026-02-17 (обновление 4)

### Исправлено
- Структурные облигации теперь действительно исключаются из итоговой выгрузки (добавлен отдельный фильтр после обогащения эмитентами).
- Исправлен расчёт `HAS_PUT_CALL_OFFER`: учитываются только актуальные оферты (с датой сегодня/в будущем). Прошедшие оферты больше не считаются активными.
- Исправлен выбор `PUT_CALL_OFFER_DATE` при нескольких офертах: используется ближайшая актуальная дата.
- Для пожизненного кэша эмитентов добавлен флаг структурного типа `secid_to_is_structural`, чтобы не переопределять его на каждом запуске.

### Изменено
- Обновлена версия схемы суточного кэша до `v6` для пересчёта оферт по новой логике.
- Обновлён `README.md` (актуальные правила по структурным бумагам и офертам).

## 2026-02-17 (обновление 3)

### Исправлено
- Усилена нормализация `BOND_TYPE`: теперь любые варианты, содержащие «структур... облигац...», приводятся к `Не указан`.
- Добавлен отдельный фильтр `COUPONPERIOD`: если после восстановления период остался `0`, бумага исключается из выгрузки.
- Исправлен парсинг оферт: теперь `HAS_PUT_CALL_OFFER` определяется по фактическому наличию записей в `offers`, а не только по английским значениям типа оферты.
- Исправлен выбор даты оферты: берётся ближайшая будущая дата, а если таких нет — последняя доступная историческая.

### Изменено
- Обновлена версия схемы суточного кэша до `v5` для автоматического пересчёта полей оферт по новым правилам.
- Обновлён `README.md` с пояснениями новой логики по `COUPONPERIOD` и офертам.

## 2026-02-17 (обновление 2)

### Добавлено
- Добавлены поля `HAS_PUT_CALL_OFFER` и `PUT_CALL_OFFER_DATE` с парсингом из блока `offers` MOEX bondization.
- В суточный кэш добавлено хранение признака и даты Put/Call оферт.
- В пожизненный кэш эмитентов добавлено хранение исправленного `COUPONPERIOD` для выпусков, где API возвращает `0`.

### Изменено
- Для `BOND_TYPE` добавлена нормализация: значения типа `Структурная облигация` теперь заменяются на `Не указан` и запоминаются, чтобы повторно не опрашивать API.
- Для выпусков с `COUPONPERIOD = 0` теперь период вычисляется по `COUPONFREQUENCY`, сохраняется в пожизненном кэше и повторно не пересчитывается.
- Обновлён лист `INFO`, README и порядок колонок в Excel с учётом полей Put/Call оферт.

### Улучшено
- Реализована идея из improvement по расширению анализа оферт: в текущем релизе добавлен автоматический сбор признака/даты оферты и подготовлена база для календаря оферт.

## 2026-02-17

### Добавлено
- Добавлено поле `BOND_TYPE` (тип облигации: фикс, флоатер и т.д.) в итоговую таблицу и лист `INFO`.
- Добавлены служебные поля маркировки для фильтра амортизации: `EXCLUDE_BY_AMORTIZATION` и `AMORTIZATION_EXCLUDE_REASON` (для внутренней логики и кэша).

### Изменено
- Исправлено определение `BOND_TYPE`: теперь тип читается из поля `BOND_TYPE` в description MOEX (ранее искалось неверное имя поля).
- Ускорен повторный запуск: кэшированные пустые типы облигаций пересчитываются один раз и сохраняются как `Не указан`, чтобы не выполнять повторные массовые запросы на каждом запуске.
- Форматы дат `MATDATE` и `AMORTIZATION_START_DATE` в Excel переведены в вид `ДД.ММ.ГГГГ`.
- Реализован фильтр: бумаги, где амортизация уже началась или начнётся менее чем через год, исключаются из выгрузки.
- Суточный кэш метрик теперь хранит флаг исключения по амортизации, чтобы не перерасходовать запросы на такие бумаги.
- Улучшен сбор MOEX: добавлена безопасная постраничная логика с остановкой при отсутствии новых ключей.

### Улучшено
- Реализована идея из `IMPROVEMENTS.md` №3: загрузка всех страниц MOEX ISS с защитой от зацикливания API.

### Добавлено
- Добавлен пожизненный кэш признака `QUALIFIED_INVESTOR` в справочник эмитентов.
- Добавлен фильтр: бумаги с датой погашения менее 1 года исключаются до этапов обогащения.
- Добавлены поля `HAS_AMORTIZATION` (✔/✖) и `AMORTIZATION_START_DATE`.
- Добавлено поле `ACCRUEDINT` (НКД) в итоговую таблицу.
- Добавлен суточный кэш `cache/daily_security_metrics_cache.json` для полей амортизации (обновление раз в 24 часа).

### Изменено
- Исправлен расчёт `HAS_AMORTIZATION`: финальное погашение (maturity) больше не считается амортизацией.
- Обогащение эмитентов теперь использует пожизненный кэш не только для `ISSUER_INN`, но и для `QUALIFIED_INVESTOR`.
- Обновлено описание листа `INFO` и порядок колонок в Excel с учётом новых полей.
- Обновлён `README.md`: добавлены понятные пояснения по новым колонкам, фильтру по сроку до погашения и типам кэша.

### Улучшено
- Добавлено версионирование суточного кэша амортизации (`schema_version`) для автоматического пересчёта после изменения правил парсинга.
- Обновлены этапы логирования (7 шагов) для более понятного отображения прогресса.
- Реализован перенос идеи из improvement: кэш признака `QUALIFIED_INVESTOR`.
